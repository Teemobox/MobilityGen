# MobilityGen 项目功能与架构文档

## 1. 项目概述

MobilityGen 是一个基于 NVIDIA Isaac Sim 构建的工具集，用于为移动机器人轻松生成和收集数据。该项目支持多种机器人类型、多种数据收集方法，并能够生成丰富的真值数据，用于训练模型和测试与机器人移动性相关的算法。

### 1.1 核心功能

- **丰富的真值数据生成**：占用地图、位姿、关节位置/速度、RGB图像、分割图像、深度图像、实例分割图像、法线图像等
- **多种机器人类型支持**：差速驱动（Jetbot、Carter）、四足机器人（Spot）、人形机器人（H1）等
- **多种数据收集方法**：手动控制（键盘遥操作、游戏手柄遥操作）、自动化（随机加速度、随机路径跟随）

## 2. 项目架构与子模块

### 2.1 数据读取模块 (Reader)

**位置**：`examples/reader.py`、`exts/omni.ext.mobility_gen/omni/ext/mobility_gen/reader.py`

**功能**：
- 从记录的数据目录中读取状态信息
- 支持读取多种类型的数据：RGB图像、深度图像、分割图像、法线图像、通用状态字典
- 提供统一的接口访问记录数据

**关键接口函数**：

- `Reader.__init__(recording_path: str)`：初始化读取器，扫描记录目录结构
- `Reader.read_config() -> Config`：读取配置文件（场景类型、机器人类型、场景USD路径）
- `Reader.read_occupancy_map() -> OccupancyMap`：读取占用地图
- `Reader.read_state_dict(index: int) -> OrderedDict`：读取完整的状态字典（包含所有传感器数据）
- `Reader.read_state_dict_common(index: int) -> dict`：读取通用状态字典（位姿、关节状态等）
- `Reader.read_state_dict_rgb(index: int) -> OrderedDict`：读取RGB图像字典
- `Reader.read_state_dict_depth(index: int) -> OrderedDict`：读取深度图像字典
- `Reader.read_state_dict_segmentation(index: int) -> OrderedDict`：读取分割图像字典
- `Reader.read_state_dict_normals(index: int) -> OrderedDict`：读取法线图像字典
- `Reader.read_rgb(name: str, index: int) -> np.ndarray`：读取指定名称的RGB图像
- `Reader.read_depth(name: str, index: int, eps=1e-6) -> np.ndarray`：读取指定名称的深度图像
- `Reader.read_segmentation(name: str, index: int) -> np.ndarray`：读取指定名称的分割图像
- `Reader.read_normals(name: str, index: int) -> np.ndarray`：读取指定名称的法线图像
- `Reader.__len__() -> int`：返回记录的时间步数
- `Reader.__getitem__(index: int)`：支持索引访问，返回状态字典

**原理**：
- 扫描记录目录结构，识别所有可用的时间步
- 根据数据类型（RGB、深度、分割、法线）从不同文件夹读取
- 深度图像使用16位PNG格式存储，需要转换为实际深度值
- 法线图像以numpy数组格式存储

### 2.2 占用地图模块 (OccupancyMap)

**位置**：`examples/occupancy_map.py`、`exts/omni.ext.mobility_gen/omni/ext/mobility_gen/occupancy_map.py`

**功能**：
- 表示和管理2D占用地图
- 支持ROS格式的占用地图读写
- 提供世界坐标与像素坐标之间的转换
- 支持地图缓冲（膨胀）操作，用于路径规划和碰撞检测

**关键接口函数**：

- `OccupancyMap.__init__(data: np.ndarray, resolution: int, origin: Tuple[int, int, int])`：初始化占用地图
- `OccupancyMap.freespace_mask() -> np.ndarray`：获取自由空间掩码
- `OccupancyMap.occupied_mask() -> np.ndarray`：获取占用区域掩码
- `OccupancyMap.unknown_mask() -> np.ndarray`：获取未知区域掩码
- `OccupancyMap.ros_image(negate: bool = False) -> PIL.Image.Image`：生成ROS格式的图像
- `OccupancyMap.ros_yaml(negate: bool = False) -> str`：生成ROS格式的YAML文件内容
- `OccupancyMap.save_ros(path: str)`：保存为ROS格式（PNG + YAML）
- `OccupancyMap.from_ros_yaml(ros_yaml_path: str) -> OccupancyMap`：从ROS YAML文件加载占用地图
- `OccupancyMap.from_ros_image(...) -> OccupancyMap`：从ROS格式图像创建占用地图
- `OccupancyMap.from_masks(freespace_mask, occupied_mask, resolution, origin) -> OccupancyMap`：从掩码创建占用地图
- `OccupancyMap.buffered(buffer_distance_pixels: int) -> OccupancyMap`：对占用区域进行膨胀（缓冲）
- `OccupancyMap.buffered_meters(buffer_distance_meters: float) -> OccupancyMap`：以米为单位的缓冲操作
- `OccupancyMap.pixel_to_world(point: Point2d) -> Point2d`：像素坐标转世界坐标
- `OccupancyMap.pixel_to_world_numpy(points: np.ndarray) -> np.ndarray`：批量像素坐标转世界坐标
- `OccupancyMap.world_to_pixel_numpy(points: np.ndarray) -> np.ndarray`：批量世界坐标转像素坐标
- `OccupancyMap.check_world_point_in_bounds(point: Point2d) -> bool`：检查世界坐标是否在地图范围内
- `OccupancyMap.check_world_point_in_freespace(point: Point2d) -> bool`：检查世界坐标是否在自由空间

**原理**：
- 使用三种状态值：UNKNOWN (0)、FREESPACE (1)、OCCUPIED (2)
- 支持ROS占用地图标准格式，包括图像和YAML元数据
- 坐标转换基于地图分辨率和原点位置进行线性变换
- 缓冲操作使用OpenCV的形态学膨胀，创建圆形核来模拟机器人碰撞半径

### 2.3 机器人模块 (Robot)

**位置**：`exts/omni.ext.mobility_gen/omni/ext/mobility_gen/robots.py`

**功能**：
- 定义机器人的抽象基类
- 管理机器人的物理属性、传感器配置、控制参数
- 提供统一的接口用于机器人构建、动作执行、状态读取

**关键接口函数**：

- `Robot.__init__(prim_path, robot, articulation_view, front_camera)`：初始化机器人
- `Robot.build(prim_path: str) -> Robot`：构建机器人（抽象方法，子类实现）
- `Robot.write_action(step_size: float)`：执行动作（抽象方法，子类实现）
- `Robot.build_front_camera(prim_path)`：构建前置相机
- `Robot.build_chase_camera(prim_path)`：构建跟随相机
- `Robot.set_pose_2d(pose: Pose2d)`：设置机器人2D位姿
- `Robot.get_pose_2d() -> Pose2d`：获取机器人2D位姿
- `Robot.update_state()`：更新机器人状态（位置、方向、关节状态）
- `Robot.read_state_dict_common() -> dict`：读取通用状态字典
- `Robot.read_state_dict_rgb() -> dict`：读取RGB图像字典
- `Robot.read_state_dict_depth() -> dict`：读取深度图像字典
- `Robot.read_state_dict_segmentation() -> dict`：读取分割图像字典
- `Robot.read_state_dict_normals() -> dict`：读取法线图像字典
- `Robot.read_state_dict() -> dict`：读取完整状态字典

**原理**：
- 使用Isaac Sim的Articulation API控制机器人关节
- 通过Buffer机制管理状态数据，支持标签系统用于数据分类
- 支持多种控制模式：差速驱动、全向移动、人形机器人控制
- 相机传感器通过Isaac Replicator进行渲染和数据采集

**支持的机器人类型**：
- `H1Robot`：人形机器人，使用H1FlatTerrainPolicy进行控制
- `SpotRobot`：四足机器人，使用SpotFlatTerrainPolicy进行控制
- `JetbotRobot`：差速驱动机器人
- `CarterRobot`：差速驱动机器人

### 2.4 场景模块 (Scenario)

**位置**：`exts/omni.ext.mobility_gen/omni/ext/mobility_gen/scenarios.py`

**功能**：
- 定义数据收集场景的抽象基类
- 实现多种数据收集方法：手动控制、随机动作、路径跟随
- 管理场景的初始化和步进逻辑

**关键接口函数**：

- `Scenario.__init__(robot: Robot, occupancy_map: OccupancyMap)`：初始化场景
- `Scenario.reset()`：重置场景（抽象方法，子类实现）
- `Scenario.step(step_size: float) -> bool`：执行一步场景（抽象方法，子类实现）
- `Scenario.update_state()`：更新状态

**场景类型**：

1. **KeyboardTeleoperationScenario**（键盘遥操作场景）
   - `reset()`：随机采样机器人初始位姿
   - `step(step_size)`：根据键盘输入（W/A/S/D）控制机器人移动

2. **GamepadTeleoperationScenario**（游戏手柄遥操作场景）
   - `reset()`：随机采样机器人初始位姿
   - `step(step_size)`：根据游戏手柄输入控制机器人移动

3. **RandomAccelerationScenario**（随机加速度场景）
   - `reset()`：在网格上随机采样初始位姿
   - `step(step_size)`：使用随机加速度模型生成动作，检测碰撞并自动重置

4. **RandomPathFollowingScenario**（随机路径跟随场景）
   - `reset()`：采样初始位姿，生成随机路径
   - `step(step_size)`：使用路径跟随控制器跟踪路径，到达终点或碰撞时重置

**原理**：
- 使用位姿采样器（PoseSampler）在占用地图上采样有效位姿
- 路径跟随使用A*路径规划器生成路径，然后使用纯跟踪控制器跟随
- 碰撞检测基于缓冲后的占用地图进行
- 支持GPU加速的路径规划（可选）

### 2.5 传感器模块 (Sensor)

**位置**：`exts/omni.ext.mobility_gen/omni/ext/mobility_gen/sensors.py`

**功能**：
- 定义传感器抽象基类
- 实现相机传感器，支持多种数据类型的采集
- 管理渲染管线和数据标注器

**关键接口函数**：

- `Sensor.build(prim_path: str)`：构建传感器（抽象方法）
- `Sensor.attach(prim_path: str)`：附加传感器（抽象方法）
- `Camera.__init__(prim_path, resolution)`：初始化相机
- `Camera.enable_rendering()`：启用渲染
- `Camera.disable_rendering()`：禁用渲染
- `Camera.enable_rgb_rendering()`：启用RGB渲染
- `Camera.enable_segmentation_rendering()`：启用分割渲染
- `Camera.enable_instance_id_segmentation_rendering()`：启用实例ID分割渲染
- `Camera.enable_depth_rendering()`：启用深度渲染
- `Camera.enable_normals_rendering()`：启用法线渲染
- `Camera.update_state()`：更新相机状态（位置、方向、图像数据）
- `HawkCamera.build(prim_path: str) -> HawkCamera`：构建Hawk相机（类方法）

**原理**：
- 使用Isaac Replicator创建渲染产品（Render Product）
- 通过不同的标注器（Annotator）获取不同类型的数据：
  - LdrColor：RGB图像
  - SemanticSegmentation：语义分割
  - InstanceSegmentation：实例分割
  - DistanceToCamera：深度图像
  - Normal：法线图像
- 数据通过Buffer机制存储，支持标签分类

### 2.6 数据写入模块 (Writer)

**位置**：`exts/omni.ext.mobility_gen/omni/ext/mobility_gen/writer.py`

**功能**：
- 将状态数据写入文件系统
- 支持多种数据格式的保存：numpy数组、图像文件、配置文件
- 管理记录目录结构

**关键接口函数**：

- `Writer.__init__(path: str)`：初始化写入器
- `Writer.write_state_dict_common(state_dict: dict, step: int)`：写入通用状态字典（.npy格式）
- `Writer.write_state_dict_rgb(state_rgb: dict, step: int)`：写入RGB图像（.jpg格式）
- `Writer.write_state_dict_segmentation(state_segmentation: dict, step: int)`：写入分割图像（.png格式）
- `Writer.write_state_dict_depth(state_np: dict, step: int)`：写入深度图像（16位PNG，逆深度编码）
- `Writer.write_state_dict_normals(state_np: dict, step: int)`：写入法线图像（.npy格式）
- `Writer.write_stage()`：保存USD场景文件
- `Writer.copy_stage(input_path: str)`：复制USD场景文件
- `Writer.write_config(config: Config)`：写入配置文件（JSON格式）
- `Writer.write_occupancy_map(occupancy_map: OccupancyMap)`：写入占用地图（ROS格式）

**原理**：
- 按数据类型组织目录结构：`state/common/`、`state/rgb/`、`state/depth/`等
- 使用8位数字格式命名文件（如`00000000.npy`）表示时间步
- 深度图像使用逆深度编码以16位PNG格式存储，节省空间
- RGB图像使用JPEG格式压缩，分割图像使用PNG格式保持精度

### 2.7 位姿采样器模块 (PoseSampler)

**位置**：`exts/omni.ext.mobility_gen/omni/ext/mobility_gen/pose_samplers.py`

**功能**：
- 在占用地图上采样有效的机器人位姿
- 支持多种采样策略：均匀采样、网格采样

**关键接口函数**：

- `PoseSampler.sample_px(occupancy_map: OccupancyMap) -> Pose2d`：在像素坐标中采样位姿（抽象方法）
- `PoseSampler.sample(occupancy_map: OccupancyMap) -> Pose2d`：在世界坐标中采样位姿
- `UniformPoseSampler.sample_px(occupancy_map) -> Pose2d`：均匀采样自由空间中的任意点
- `GridPoseSampler.sample_px(occupancy_map) -> Pose2d`：在网格区域中采样位姿

**原理**：
- 均匀采样：从所有自由空间像素中随机选择，角度随机
- 网格采样：将地图划分为网格，先选择网格区域，再在区域内采样，确保空间分布更均匀

### 2.8 场景构建器模块 (SceneBuilder)

**位置**：`exts/omni.ext.mobility_gen/omni/ext/mobility_gen/scene_builders.py`

**功能**：
- 构建和配置3D场景
- 支持从USD文件加载场景或程序化生成场景

**关键接口函数**：

- `SceneBuilder.build(prim_path: str)`：构建场景（抽象方法）
- `RandomCubeScene.build(prim_path: str)`：构建随机立方体场景

**原理**：
- 可以从USD文件引用加载预定义场景
- 程序化场景生成：随机放置几何体、设置光照等

### 2.9 路径规划模块 (PathPlanner)

**位置**：`path_planner/`

**功能**：
- 提供A*路径规划算法的C++实现
- 支持GPU加速的最近点查找（可选）
- 生成从起点到所有可达点的路径图

**关键接口函数**：

- `generate_paths(start: Tuple[int, int], freespace: np.ndarray) -> PathOutput`：生成所有路径
- `PathOutput.unroll_path(end: Tuple[int, int]) -> List[Tuple[int, int]]`：展开到指定终点的路径
- `PathOutput.sample_random_end_point() -> Tuple[int, int]`：随机采样终点

**原理**：
- 使用Dijkstra算法从起点探索所有可达点
- 构建路径图数据结构，支持快速路径查询
- GPU加速版本使用CUDA进行并行最近点查找

### 2.10 工具模块 (Utils)

**位置**：`exts/omni.ext.mobility_gen/omni/ext/mobility_gen/utils/`

**功能**：
- 提供各种工具函数和辅助类

**主要工具模块**：

1. **global_utils.py**：全局工具函数
   - `get_stage()`：获取USD舞台
   - `get_world()`：获取物理世界
   - `save_stage(path: str)`：保存舞台

2. **stage_utils.py**：舞台操作工具
   - `stage_add_camera(...)`：添加相机
   - `stage_add_usd_ref(...)`：添加USD引用
   - `stage_add_dome_light(...)`：添加环境光

3. **prim_utils.py**：图元操作工具
   - `prim_translate(...)`：平移图元
   - `prim_rotate_x/y/z(...)`：旋转图元
   - `join_prim_path(...)`：连接路径

4. **path_utils.py**：路径处理工具
   - `PathHelper`：路径辅助类，用于路径跟随控制
   - `vector_angle(...)`：计算向量角度

5. **math_utils.py**：数学工具函数

6. **occupancy_map_utils.py**：占用地图工具函数

7. **registry.py**：注册表系统
   - `Registry[T]`：类型化注册表，用于注册机器人、场景等

## 3. 数据格式

### 3.1 记录目录结构

```
recording_name/
├── occupancy_map/
│   ├── map.png          # ROS格式占用地图图像
│   └── map.yaml         # ROS格式占用地图元数据
├── config.json          # 配置信息（场景类型、机器人类型、场景USD路径）
├── stage.usd            # USD场景文件
└── state/
    ├── common/          # 通用状态数据（.npy格式）
    │   ├── 00000000.npy
    │   ├── 00000001.npy
    │   └── ...
    ├── rgb/             # RGB图像（.jpg格式）
    │   ├── robot.front_camera.left.rgb_image/
    │   │   ├── 00000000.jpg
    │   │   └── ...
    │   └── robot.front_camera.right.rgb_image/
    │       └── ...
    ├── depth/           # 深度图像（16位PNG，逆深度编码）
    │   ├── robot.front_camera.left.depth_image/
    │   │   ├── 00000000.png
    │   │   └── ...
    │   └── ...
    ├── segmentation/    # 分割图像（.png格式）
    │   ├── robot.front_camera.left.segmentation_image/
    │   │   ├── 00000000.png
    │   │   └── ...
    │   └── ...
    └── normals/         # 法线图像（.npy格式）
        ├── robot.front_camera.left.normals_image/
        │   ├── 00000000.npy
        │   └── ...
        └── ...
```

### 3.2 状态字典结构

`read_state_dict(index)` 返回的字典包含以下键：

```python
{
    # 机器人动作
    "robot.action": np.ndarray,  # [2] - 线速度、角速度命令
    
    # 机器人位姿
    "robot.position": np.ndarray,  # [3] - XYZ位置
    "robot.orientation": np.ndarray,  # [4] - 四元数方向
    
    # 关节状态
    "robot.joint_positions": np.ndarray,  # [J] - 关节位置
    "robot.joint_velocities": np.ndarray,  # [J] - 关节速度
    
    # 相机数据（左相机示例）
    "robot.front_camera.left.rgb_image": np.ndarray,  # [HxWx3], uint8 - RGB图像
    "robot.front_camera.left.depth_image": np.ndarray,  # [HxW], float32 - 深度（米）
    "robot.front_camera.left.segmentation_image": np.ndarray,  # [HxW], uint8 - 分割类别索引
    "robot.front_camera.left.segmentation_info": dict,  # 分割信息（Isaac Replicator格式）
    "robot.front_camera.left.instance_id_segmentation_image": np.ndarray,  # [HxW], uint8 - 实例ID
    "robot.front_camera.left.instance_id_segmentation_info": dict,  # 实例分割信息
    "robot.front_camera.left.normals_image": np.ndarray,  # [HxWx3], float32 - 法线向量
    "robot.front_camera.left.position": np.ndarray,  # [3] - 相机世界位置
    "robot.front_camera.left.orientation": np.ndarray,  # [4] - 相机世界方向（四元数）
    
    # 右相机数据（类似结构）
    "robot.front_camera.right.rgb_image": np.ndarray,
    # ...
}
```

## 4. 使用示例

### 4.1 读取状态数据

```python
from reader import Reader

reader = Reader(recording_path="path/to/recording")
state = reader.read_state_dict(index=0)
print(state.keys())  # 打印所有可用的键
```

### 4.2 可视化图像数据

```python
import matplotlib.pyplot as plt
from reader import Reader

reader = Reader(recording_path="path/to/recording")
images = reader.read_state_dict(index=0)

plt.subplot(2, 2, 1)
plt.imshow(images['robot.front_camera.left.rgb_image'])
plt.title('Left RGB')

plt.subplot(2, 2, 2)
plt.imshow(1.0 / images['robot.front_camera.left.depth_image'])
plt.title('Left Depth (inverse)')
```

### 4.3 可视化轨迹

```python
import matplotlib.pyplot as plt
import numpy as np
from reader import Reader

reader = Reader(recording_path="path/to/recording")
occupancy_map = reader.read_occupancy_map()

points_world = []
for index in range(len(reader)):
    state_dict = reader.read_state_dict_common(index)
    position = state_dict['robot.position']
    points_world.append(position[0:2])

points_world = np.array(points_world)
points_image = occupancy_map.world_to_pixel_numpy(points_world)

plt.imshow(occupancy_map.freespace_mask(), cmap="gray")
plt.plot(points_image[:, 0], points_image[:, 1], 'g-')
plt.show()
```

### 4.4 交互式数据浏览器

使用Gradio创建Web界面浏览数据：

```python
python examples/04_visualize_gradio.py
```

## 5. 扩展开发

### 5.1 实现自定义机器人

1. 继承 `Robot` 类
2. 实现 `build()` 方法：添加机器人到USD舞台
3. 实现 `write_action()` 方法：执行动作逻辑
4. 设置类参数（物理时间步、占用地图参数等）
5. 使用 `@ROBOT.register()` 装饰器注册

### 5.2 实现自定义场景

1. 继承 `Scenario` 类
2. 实现 `reset()` 方法：初始化场景
3. 实现 `step()` 方法：执行一步场景逻辑
4. 使用 `@SCENARIOS.register()` 装饰器注册

## 6. 技术特点

1. **模块化设计**：使用注册表系统实现可扩展的架构
2. **高效数据存储**：深度图像使用逆深度编码，RGB使用JPEG压缩
3. **GPU加速支持**：路径规划支持CUDA加速（可选）
4. **ROS兼容**：占用地图支持ROS标准格式
5. **丰富的传感器数据**：支持RGB、深度、分割、实例分割、法线等多种数据类型

## 7. 关键依赖

- **NVIDIA Isaac Sim 4.5.0**：核心仿真平台
- **Isaac Replicator**：渲染和数据标注
- **NumPy**：数值计算
- **PIL/Pillow**：图像处理
- **OpenCV**：图像处理和形态学操作
- **Gradio**：Web界面（可选）
- **CUDA Toolkit**：GPU加速（可选）

---

本文档基于项目代码和示例程序编写，详细说明了MobilityGen项目的功能、架构和各子模块的实现原理。


